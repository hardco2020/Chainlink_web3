import React from "react";

const style = {
  wrapper: `w-full h-full bg-slate-500 `,
};

const StreamElement = () => {
  return <div className={style.wrapper}>StreamElement</div>;
};

export default StreamElement;
